
## [08:44:28] refactor(main): _count_unpushed_commits → git_helpers.git_count_ahead

## 动机
util/git_helpers.py 顶部注释 (v0.3 refactor) 列了 3 处 `git rev-list --count` 重复调用点：
  - side_git.py::SideGit._run ✅ 已迁
  - side_git.py::SideGit.push ✅ 已迁
  - main.py::cmd_autonomous._count_unpushed_commits ❌ **本次迁**

inline 实现 (~10 行) 与 `git_count_ahead(cwd)` 完全等价，重复维护一份会漂移。

## 改动
- 替换 import：`from .util.subprocess_utils import run_capture` → `from .util.git_helpers import git_count_ahead`
- `_count_unpushed_commits` body 从 inline `run_capture(...) + int(out.strip() or 0) if rc >= 0 else 0` 收缩为 `return git_count_ahead(cwd=s.repo_root)`
- 同步更新 docstring 指明已迁移版本

## 验证
- `pytest tests/test_subprocess_utils.py tests/test_git_tools.py tests/test_git_tools_timeout.py tests/test_sidegit.py tests/test_sidegit_cleanup.py tests/test_backend_minimaxi.py tests/test_autonomous.py` → 88 passed in 5.43s（baseline 同样 88 passed）
- `import xragent.main` + `inspect.getsource(cmd_autonomous)` 确认 closure 内 `run_capture(` 调用已消失、`git_count_ahead` 已接入
- 3 个 pre-existing collection error (test_evolve_tools / test_memory_tools_title / test_runtime_state_dedup) 与本次改动无关，跳过

## 影响
- 公开 API 不变（`cmd_autonomous` 行为、签名、退出码、stdout 文案完全一致）
- `_count_unpushed_commits` 是 nested closure，外部不可见（之前测试也没 import 它）
- `run_capture` 不再被 main.py 引用，但 side_git.py / tests 仍用 — util 自身保留

## diff stat
- src/xragent/main.py: -8 行 (444 → 436)
